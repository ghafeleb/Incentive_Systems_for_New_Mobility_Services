---
step_size: 0.01
start_hour_opt: 5  # 5 AM  # Hour of the start of the optimization time
n_times_test_horizon: 204 # 1 time= 1 time interval (for example 5 minutes)
theta_temp: 1  # Temperature of Softmax function in computing choice probability
fileNameSetting1: 5_22_AVG5_th1_pad_MultipleTimes
region_: 'region_toy'  # region of data (Do not change)
pad_str:  '_pad'  # '_pad' if we want data with padding
n_hours: 17  # 3 AM - 10 PM  # Number of hours of data
start_hour: 5  # 3 AM  # Starting hour of data without padding
start_minute_opt: 0  # 0 minutes  # Minute of the start of the optimization time
interval_t: 5  # 15-minute intervals  # Time interval in data (minutes)
analysis_month: 5  # Month of the date of the analysis data
analysis_day:  1     # Day of the date of the analysis data
num_paths_temp: 6  # Maximum number of paths from origin to destination
plot_: True  # If True: show the plots
OD: ''  # Set of OD pairs
